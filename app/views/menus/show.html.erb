<div class="container">
  <div class="top-margin">
    <div class="page-header">
      <%= link_to menus_path, class: 'btn btn-warning' do %>
        <span class="glyphicon glyphicon-list-alt"></span>
        Back
      <% end %>
      <%= link_to edit_menu_path(@menu), class: 'btn btn-primary' do %>
        <span class="glyphicon glyphicon-pencil"></span>
        Edit
      <% end %>
      <h1 class="center-text">Show menu</h1>
    </div>

    <dl class="dl-horizontal">

      <dt>Place:</dt>
      <dd><%= @menu.place_id %></dd>

      <dt>Menu date:</dt>
      <dd><%= @menu.menu_date %></dd>

    </dl> 

        <%= link_to "Add Dish", new_dish_path(menu_id: @menu.id) %>
  </div>
</div>